import React from "react";
import { Contact } from "@/components/contact";

const page = () => {
  return (
    <div className="w-full xl:w-[80%] mx-auto">
      <Contact />
    </div>
  );
};

export default page;
